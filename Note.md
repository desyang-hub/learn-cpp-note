11. 类对象在内存里大致长什么样？

对于普通类，对象内存空间按照成员变量的声明顺序排列，而对于含有虚函数的类，对象最前面还包含一个vptr(虚指针)指向虚函数表，虚函数表中包含真正需要调用的函数的位置从而实现多态。
静态成员变量不在对象中在数据区，成员函数不在对象中在代码段

12. 虚函数的实现原理？vptr 和 vtable 是什么？

虚函数实现是通过vptr和vtable实现的
对于含有虚函数类对象的最前头会有各vptr虚指针，虚指针指向vtable，vtable是属于类的，其中包含实际调用的虚函数的地址，这样的话，使用父类指针指向基类对象，调用虚函数过程中，虚指针并未发生改变，所以调用的依然是子类vtable中所指向的函数，这就是多态